import type { ConceptProps } from "../types"

// const baseUrl = import.meta.env.VITE_BACKEND_URI as string

const useConceptNew = async (props: ConceptProps): Promise<void> => {
  // const url = `${baseUrl}/concepts`
  // return await axios.post(url, props)
}

export default useConceptNew
